# Tests

The test suite is located in the `contracts/src/__tests__` directory, covering circuit logic, ledger state transitions, and verification that private inputs are not exposed.

To run the tests, execute `npm test` from the root directory, which will run the vitest suites in the workspace.
